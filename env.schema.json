{
  "vars": [
    {
      "name": "TEMPLATE_UPSTREAM",
      "required": false,
      "description": "Override upstream owner/repo for template update checks"
    },
    {
      "name": "API_BASE_URL",
      "required": false,
      "description": "Child-app API base URL"
    },
    {
      "name": "LOG_LEVEL",
      "required": false,
      "description": "Child-app log verbosity"
    },
    {
      "name": "RENDER_BACKEND",
      "required": false,
      "description": "Icon pack renderer: dry-run (CI default) or blender"
    },
    {
      "name": "BLENDER_BIN",
      "required": false,
      "description": "Absolute path to official Blender binary with OptiX"
    },
    {
      "name": "CYCLES_DEVICE",
      "required": false,
      "description": "Cycles device: OPTIX or CUDA"
    },
    {
      "name": "HOVER_ICONS_GPU",
      "required": false,
      "description": "Set to 1 to enable local GPU pytest marks"
    }
  ]
}
