[project]
name = "golden-path-python"
version = "0.1.0"
description = "Golden Path Python CLI stub plus Hover Icons catalog renderer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.2",
]

[project.scripts]
hello = "hello.cli:main"

[project.optional-dependencies]
dev = [
    "hypothesis>=6.168.0",
    "mypy>=1.13.0",
    "pip-licenses>=4.5.0",
    "pytest>=8.3.0",
    "pytest-cov>=6.0.0",
    "pytest-xdist>=3.8.0",
    "ruff>=0.16.7",
    "pyright>=1.1.414",
    "types-PyYAML>=6.0.12",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hello", "src/iconpack"]

[tool.hatch.build.targets.sdist]
include = ["src/hello", "src/iconpack", "tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
strict = true
python_version = "3.11"
# About + crash modules stay under strict (hello.about, hello.crash) — no per-module relax.

[tool.pytest.ini_options]
testpaths = ["tests"]
# -n auto uses local CPU cores (pytest-xdist); override with PYTEST_ADDOPTS or -n N
addopts = "-n auto --cov=hello --cov=iconpack --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
source = ["hello", "iconpack"]
omit = [
    "src/iconpack/backend_blender.py",
    "src/iconpack/__main__.py",
    "src/iconpack/validate.py",
]

[tool.coverage.report]
fail_under = 90

[tool.pyright]
include = ["src"]
typeCheckingMode = "basic"
